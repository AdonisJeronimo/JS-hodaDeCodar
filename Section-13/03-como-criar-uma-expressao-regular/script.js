/*
COMO CRIAR UMA EXPRESSÃO REGULAR

As expressões regulares no JS são um tipo de objeto, onde podemos instancia-la de duas formas:

    New RegExp ou /../;
*/

let reg1 = new RegExp("test");
let reg2 = /teste/;

// Em ambos os casos temos a palavra test, que é o padrão que queremos encontrar. 