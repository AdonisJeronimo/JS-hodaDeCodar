/*
ARRAYS

Os arrays possibilidam que adicionemos um conjunto de valores a uma variavel. O array deve ser escrito
entre colchetes, separando os valores por virgula, veja:
*/

let numeros = [1,3,5,8,12];

let nomes = ["João", "Matheus", "Alex" ];

let bool = [true,false,true,false,true,false]

let informacoes = [ "Adonis", 24, true, "Teste", 2, false];

/*
Não existem restrições em relação ao tipo de dado, mas geralmente mantemos um pardão ao inves de misturar.
 Também, conseguimos mostrar Arrays no console
*/

console.log(numeros);
console.log(nomes);
console.log(bool);
console.log(informacoes);
